spring.config.import=optional:file:.env[.properties]

spring.datasource.url=${MYSQL_URL}
spring.datasource.username=${MYSQL_USERNAME}
spring.datasource.password=${MYSQL_PASSWORD}

jwt_secret=${JWT_SECRET}

app.upload.dir=C:/sinchan/uploads/

logging.file.name=sinchan_service_logs.log

spring.messages.basename=messages/message

#spring.main.banner-mode=off

#mail properties
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=ksb17219@gmail.com
spring.mail.password=pknl gobc uzlg sxdx
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# For Spring Boot 2.x/3.x with embedded Tomcat
spring.servlet.multipart.enabled=true

spring.servlet.multipart.max-file-size=150MB
spring.servlet.multipart.max-request-size=150MB

# Additional Tomcat-specific settings
server.tomcat.max-http-form-post-size=150MB